# MetalLB - minimal config for kind cluster on OrbStack/Docker.
# IPAddressPool and L2Advertisement are applied separately via metallb:configure.
speaker:
  frr:
    enabled: false

frr-k8s:
  enabled: false
  prometheus:
    serviceMonitor:
      enabled: false
